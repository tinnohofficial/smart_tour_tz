/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "jyquznoodnlreg99.public.blob.vercel-storage.com",
            "example.com",
            "localhost",
        ]
    }
};

export default nextConfig;
